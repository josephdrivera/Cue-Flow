import CueForm from '@/components/shows/cue-form';

export default function CuesPage({
  params,
}: {
  params: { id: string };
}) {
  return (
    <div>
      {/* TODO: Implement cue management interface */}
    </div>
  );
}